{
  "name": "pipepile",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --base=/pipepile",
    "build": "cross-env NODE_ENV=production astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "deploy": "cross-env NODE_ENV=production astro build && node scripts/deploy.mjs"
  },
  "dependencies": {
    "astro": "^6.4.7",
    "embla-carousel": "^8.6.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "sass-embedded": "^1.100.0",
    "vite-plugin-svgo": "^2.0.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
