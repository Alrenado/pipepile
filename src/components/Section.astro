---
interface Props {
    class?: string
    id?: string
    fullWidth?: boolean
    paddingTop?: string
}
const { class: className, id, fullWidth = false, paddingTop } = Astro.props
---

<section {id} class={`section ${className ?? ''}`} style={paddingTop ? `padding-top: ${paddingTop}` : '20rem'}>
    <div class={`section__container ${fullWidth ? 'section__container--full' : ''}`}>
        <slot />
    </div>
</section>

<style lang="scss">
  @use '../styles/abstracts/variables' as *;

  .section {

    &__container {
      display: flex;
      max-width: $container-max;
      margin: 0 auto;
      padding: 0 $container-padding;

      &--full {
        max-width: 100%;
        padding: 0;
      }
    }
  }
</style>